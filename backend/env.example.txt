# ============================================
# CareerOS Backend — Environment Variables
# ============================================
# Copy this file as: backend/.env
# NEVER commit this file to git!
# ============================================

# --- REQUIRED ---

# A long random secret for signing JWTs. Generate one at: https://www.uuidgenerator.net/
JWT_SECRET="replace-with-a-long-random-string-at-least-32-chars"

# Supabase PostgreSQL database (replaces MongoDB)
# Get these from: https://supabase.com → Your Project → Settings → API
SUPABASE_URL="https://your-project-ref.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="your-service-role-secret-key-from-supabase"

# NVIDIA NIM API Key (for LLM features: resume analysis, interview, crucible)
# Get this from: https://build.nvidia.com → Login → Get API Key
NVIDIA_API_KEY="nvapi-xxxxxxxxxxxxxxxxxxxx"
NVIDIA_MODEL="meta/llama-3.1-70b-instruct"

# --- OPTIONAL (enables extra features) ---

# Firebase Google Login (only needed if using Google OAuth)
# Get from: https://console.firebase.google.com → Your Project → Project Settings → General
FIREBASE_PROJECT_ID="your-firebase-project-id"

# Real Job Listings via Adzuna API
# Get from: https://developer.adzuna.com/admin/applications
ADZUNA_APP_ID=""
ADZUNA_APP_KEY=""

# GitHub Token for higher-rate repo analysis in RoastMyRepo
# Get from: https://github.com/settings/tokens → Generate new token (classic) → select repo scope
GITHUB_TOKEN=""

# CORS — set to your frontend URL in production (e.g., https://careeros.vercel.app)
# Leave blank to allow all origins (fine for local dev)
FRONTEND_URL=""

# Server port (default: 4000)
PORT=4000
